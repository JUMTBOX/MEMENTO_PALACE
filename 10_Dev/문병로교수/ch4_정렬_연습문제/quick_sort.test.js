/**
 * @param {Array<number>} A
 * @param {number} firstIndex
 * @param {number} lastIndex
 */
function partition(A, firstIndex, lastIndex) {}

/**
 * @param {Array<number>} A
 * @param {number} firstIndex
 * @param {number} lastIndex
 */
function quickSort(A, firstIndex, lastIndex) {
  //
}

describe("quickSort Test", () => {
  test("테스트 예제 1", () => {});
  test("테스트 예제 2", () => {});
  test("테스트 예제 3", () => {});
});
