/**
 * @description 삽입 정렬 구현
 * @param N {number} 배열의 길이
 * @param Arr {Array<number>}
 * */
function solution (N, Arr) {
  return Arr;
}

describe("INSERTION SORT TEST", () => {
  test("예제 1", () => {
    expect(solution()).toEqual([]);
  });

  test("예제 2", () => {
    expect(solution()).toEqual([]);
  });

  test("예제 3", () => {
    expect(solution()).toEqual([]);
  });
});