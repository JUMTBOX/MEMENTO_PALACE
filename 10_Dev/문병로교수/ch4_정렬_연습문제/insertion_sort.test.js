/**
 * @description
 *
 * @param
 * @param
 * */
function solution () {
  return 0;
}

describe("INSERTION SORT TEST", () => {
  test("예제 1", () => {
    expect(solution()).toEqual([]);
  });

  test("예제 2", () => {
    expect(solution()).toEqual([]);
  });

  test("예제 3", () => {
    expect(solution()).toEqual([]);
  });
});