/**
 * @description `
 *
 *
 * `
 * @param {number} N
 * @param {number} K
 * @param {Array<number>} Arr
 * */
function solution(N, K, Arr) {
  return 0;
}

describe("버블 정렬 TEST", () => {
  test("예제 1번", () => {
    expect(solution(0, 0, [])).toEqual([]);
  });

  test("예제 2번", () => {
    expect(solution(0, 0, [])).toEqual([]);
  });

  test("예제 3번", () => {
    expect(solution(0, 0, [])).toEqual([]);
  });
});
